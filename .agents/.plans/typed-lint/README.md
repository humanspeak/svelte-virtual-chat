# Implementation Plans — typed-lint

Generated by the improve skill on 2026-07-09 (planned at commit `7257307`),
adapted from svelte-markdown's
`streaming-component-hardening/009-lint-test-files-and-typed-lint.md` at the
operator's request, re-derived against this repo's actual lint reality.

Initiative: close two lint blind spots — the 7 vitest unit-test files excluded
by `'**/*.test.ts'` in `eslint.config.mjs`, and the absence of type-aware
linting (no `no-floating-promises` / `no-misused-promises` anywhere) in a
promise-and-rAF-heavy library whose recent flaky-CI cycle turned on
exactly-once-awaited fixtures.

## Execution order & status

| Plan | Title                                                                         | Priority | Effort | Depends on           | Status |
| ---- | ----------------------------------------------------------------------------- | -------- | ------ | -------------------- | ------ |
| 001  | Lint the unit-test files and enable type-aware lint rules (floating promises) | P2       | M      | — (see precondition) | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- **Cross-batch precondition**: execute only after the
  `fix/overflow-anchor-follow-bottom` branch (batch `stream-swap-follow`,
  plan 002) merges or is abandoned — it actively edits
  `chatMeasurement.svelte.ts`, `SvelteVirtualChat.svelte`, and the stream-swap
  fixture, all of which this plan will surface mechanical fixes in.

## Adaptation notes (differences from the svelte-markdown source plan)

- **Smaller blast radius**: only 7 unit `.test.ts` files are unlinted here; the
  20 Playwright `.spec.ts` files don't match the ignore glob and are already
  linted (untyped). svelte-markdown had ~150 dark files.
- **Tooling contract now matches svelte-markdown** (adopted 2026-07-09, same
  session): CLAUDE.md's "Code Style & Linting" section makes Trunk the source
  of truth — never run `pnpm lint`/`pnpm format`/`npx prettier`/`npx eslint`,
  and never use `eslint-disable` comments (Trunk `trunk-ignore` inline ignores
  only, with a rationale). The repo's one pre-existing `eslint-disable-next-line`
  in `chatMeasurement.svelte.ts` was converted to `trunk-ignore` as part of
  that adoption; `src/lib/types.ts:6` is the other exemplar.
- **Type-project shape differs**: `.svelte-kit/tsconfig.json` already includes
  `tests/**`, so specs/fixtures get type info for free; but
  `playwright.config.ts` and `vitest.setup.ts` sit outside the project and need
  `projectService.allowDefaultProject`.
- **Added a lint-time STOP** (~3 min budget) because the husky pre-commit hook
  runs trunk on every commit in this repo — a slow typed-lint config directly
  taxes every future commit.

## Findings considered and rejected

- Wiring the installed-but-unregistered `eslint-plugin-unused-imports`:
  deferred to a future hygiene pass — real, but entangling it here widens the
  diff of a rules-activation change (noted in plan 001 maintenance notes).
- Importing svelte-markdown's Trunk-only command table verbatim: rejected —
  wrong for this repo's conventions (see Adaptation notes).
