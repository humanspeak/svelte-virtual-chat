# Implementation Plans — stream-swap-follow

Generated by the improve skill on 2026-07-07 (planned at commit `f47c3f6`).
Execute in the order below. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

Initiative: consumer apps stream an assistant reply into a placeholder
message, then replace it with the final document from their reactive store
(different id, sometimes a different component, sometimes as a
remove-then-add). Follow-bottom is lost during the replacement. Reproduce
red-first, then fix.

## Execution order & status

| Plan | Title                                                                               | Priority | Effort | Depends on | Status                                                            |
| ---- | ----------------------------------------------------------------------------------- | -------- | ------ | ---------- | ----------------------------------------------------------------- |
| 001  | Keep follow-bottom locked when a streamed message is replaced by its final document | P1       | M      | —          | IN PROGRESS — red pre-fix: `new-id-two-tick`; strategies: pending |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

Executor: when updating this row, also record (a) which spec variants were
red before the fix, and (b) which fix strategies from the plan were applied.

## Dependency notes

- Single-plan batch; no inter-plan dependencies.

## Findings considered and rejected

- Modifying `chatScrollPolicy.ts` to special-case replacement turbulence:
  rejected up front — the policy was hardened across recent releases with a
  dense spec net (scroll-escape, follow-drop, wheel-scroll-jump, scroll-away)
  and the suspected root cause is measurement identity, not scroll policy.
  The plan makes touching it a STOP condition rather than a strategy.
- Pruning orphaned height-cache entries in the same change: deferred —
  real but independent (memory growth over long sessions), and mixing it in
  would widen the blast radius of a P1 bug fix.
